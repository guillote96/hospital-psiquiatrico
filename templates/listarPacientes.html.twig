<!DOCTYPE html>
<html>
    <head>
        <title>Pacientes</title>
    </head>
    <body>
        <h1>Pacientes</h1>
        <ul>
        {% for item in resources %}
           <li>{{item.apellido}} {{item.nombre}} {{item.domicilio}} </li> <a href="./?action=editarPaciente&id={{ item.id }}"> Editar Paciente</a>
           <a href="./?action=eliminarPaciente&id={{ item.id }}"> Eliminar Paciente</a>
        {% endfor %}
        </ul>
    </body>
</html>